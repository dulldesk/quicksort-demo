# quicksort-demo
Quicksort demonstration written in Processing

## Installation

1. Install Processing at https://processing.org/download/
2. Download the repository
3. Rename the root folder (calendar-master) to `QuickSortDemo`
4. Run the file `QuickSortDemo.pde` via the Processing application.
